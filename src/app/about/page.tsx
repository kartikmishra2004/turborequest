import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Turbo Request – About Us",
}

export default function About() {
    return (
        <section className="min-h-screen flex justify-center items-center">
            About
        </section>
    );
}