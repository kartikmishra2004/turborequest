import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Turbo Request – Docs",
}

export default function Docs() {
    return (
        <section className="min-h-screen flex justify-center items-center">
            Docs
        </section>
    );
}